name: Javascript client

on:
  push:
    paths:
      - "package.json"
      - "isso-rs/static/js/**"
      - ".github/workflows/javascript-client.yml"
  pull_request:
    paths:
      - "package.json"
      - "isso-rs/static/js/**"
      - ".github/workflows/javascript-client.yml"

jobs:
  test:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        node-version: [22]
      fail-fast: false

    steps:

    - name: Check out repository code
      uses: actions/checkout@v3

    - name: Set up NodeJS ${{ matrix.node-version }} on ${{ matrix.os }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: package.json

    - name: Install Javascript test dependencies using npm
      env:
        ENV PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: true
      run: npm install

    - name: Run Javascript Jest test suite - unit tests
      run: npm run test-unit

  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
        node-version: [22]
      fail-fast: false

    steps:

    - name: Check out repository code
      uses: actions/checkout@v3

    - name: Set up NodeJS ${{ matrix.node-version }} on ${{ matrix.os }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        cache-dependency-path: package.json

    - name: Install Javascript dependencies using npm
      run: make init

    - name: Create Javascript client files
      run: make js

    - name: Archive and upload generated minified client files
      uses: actions/upload-artifact@v4
      with:
        name: client-js-files
        path: |
          isso-rs/static/js/embed.*.js
          isso-rs/static/js/count.*.js
          isso-rs/static/js/embed.dev.js.map
          isso-rs/static/js/count.dev.js.map
        if-no-files-found: error
